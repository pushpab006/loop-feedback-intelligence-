import { checkExpression, col as col$1, fn as fn$1, foreignKey, lit as lit$1, primaryKey, unique } from "@prisma/orm-family-sql/relational-core/contract-free";
import { placeholder } from "@prisma/orm-framework/errors/migration";
import { MigrationCLI } from "@prisma/orm-toolchain/cli/migration-cli";
//#region ../../../3-targets/3-targets/postgres/dist/migration.mjs
/**
* Identity factory for an already-materialized `SqlMigrationPlanOperation`.
*
* The planner uses this via `liftOpToCall` to carry ops produced by SQL
* family methods, codec control hooks, and component database dependencies
* alongside migration IR without reverse-engineering them. Users writing
* raw migrations can pass a full op shape directly — typically built by
* composing SQL family helpers — to author a migration that bypasses the
* structured call classes.
*/
function rawSql(op) {
	return op;
}
//#endregion
export { foreignKey as a, primaryKey as c, fn$1 as i, rawSql as l, checkExpression as n, lit$1 as o, col$1 as r, placeholder as s, MigrationCLI as t, unique as u };

//# sourceMappingURL=migration-PTHAY9Cv.mjs.map