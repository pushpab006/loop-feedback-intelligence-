import { OpFactoryCall } from "@prisma/orm-framework/components/control";
import { ImportSpecifierResolver } from "@prisma/orm-framework/components/emission";
//#region ../../../3-targets/3-targets/postgres/dist/render-typescript.d.mts
//#region src/core/migrations/render-typescript.d.ts
interface RenderMigrationMeta {
  readonly from: string | null;
  readonly to: string;
  /** POSIX-relative path from the migration package dir to `migrations/snapshots`, e.g. '../../snapshots'. */
  readonly snapshotsImportPath: string;
  /**
   * Rewrites the package names the scaffold imports from, for the import root
   * the consuming application installed. Applied once to the assembled
   * requirement list rather than at each `OpFactoryCall`, so a new operation
   * cannot forget it. Defaults to leaving specifiers as authored.
   */
  readonly resolveImportSpecifier?: ImportSpecifierResolver;
}
declare function renderCallsToTypeScript(calls: ReadonlyArray<OpFactoryCall>, meta: RenderMigrationMeta): string;
//#endregion
export { renderCallsToTypeScript as t };
//# sourceMappingURL=render-typescript-6sCFvMBh.d.mts.map