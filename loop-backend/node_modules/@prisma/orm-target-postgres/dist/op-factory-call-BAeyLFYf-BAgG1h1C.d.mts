import { t as PostgresRlsPolicy } from "./postgres-rls-policy-CnDxf3z9-C3LIHAIG.mjs";
import "./types-ue6jkJyF-BQJzjx1P.mjs";
import { t as PostgresPlanTargetDetails } from "./planner-target-details-HkP4RrRO-_xPQVPF0.mjs";
import { DdlColumn, DdlTableConstraint } from "@prisma/orm-family-sql/relational-core/ast";
import { ReferentialAction, StorageColumn, StorageTypeInstance } from "@prisma/orm-family-sql/contract/types";
import { MigrationOperationClass, OpFactoryCall } from "@prisma/orm-framework/components/control";
import { CodecControlHooks, SqlMigrationPlanOperation } from "@prisma/orm-family-sql/family/control";
import { ImportRequirement, TsExpression } from "@prisma/orm-framework/ts-render";
import { ExecuteRequestLowerer, Lowerer } from "@prisma/orm-family-sql/family/control-adapter";
//#region ../../../3-targets/3-targets/postgres/dist/op-factory-call-BAeyLFYf.d.mts
//#region src/core/migrations/operations/shared.d.ts
type Op$1 = SqlMigrationPlanOperation<PostgresPlanTargetDetails>;
/**
 * Literal-args shape for a foreign key definition. `references.schema`
 * carries the target table's namespace (schema) coordinate so the rendered
 * DDL qualifies the REFERENCES clause correctly for cross-schema FKs.
 */
interface ForeignKeySpec {
  readonly name: string;
  readonly columns: readonly string[];
  readonly references: {
    readonly schema: string;
    readonly table: string;
    readonly columns: readonly string[];
  };
  readonly onDelete?: ReferentialAction;
  readonly onUpdate?: ReferentialAction;
}
//#endregion
//#region src/core/migrations/operations/indexes.d.ts
interface CreateIndexExtras {
  readonly type?: string;
  readonly options?: Record<string, unknown>;
  /**
   * Partial-index predicate (WHERE body, without the keyword). Inserted
   * verbatim, never quoted or escaped — the same opaque-SQL stance as RLS
   * policy predicates.
   */
  readonly where?: string;
  readonly unique?: boolean;
}
/**
 * The element list between the parens of CREATE INDEX: either a column
 * tuple (each identifier quoted) or one opaque expression string covering
 * the entire list, inserted verbatim.
 */
type CreateIndexElements = {
  readonly columns: readonly string[];
} | {
  readonly expression: string;
};
//#endregion
//#region src/core/migrations/op-factory-call.d.ts
type Op = SqlMigrationPlanOperation<PostgresPlanTargetDetails>;
declare abstract class PostgresOpFactoryCallNode extends TsExpression implements OpFactoryCall {
  abstract readonly factoryName: string;
  abstract readonly operationClass: MigrationOperationClass;
  abstract readonly label: string;
  abstract toOp(lowerer?: Lowerer): Op | Promise<Op>;
  importRequirements(): readonly ImportRequirement[];
  protected freeze(): void;
}
declare class CreateTableCall extends PostgresOpFactoryCallNode {
  readonly factoryName: "createTable";
  readonly operationClass: "additive";
  readonly schemaName: string;
  readonly tableName: string;
  readonly columns: readonly DdlColumn[];
  readonly constraints: readonly DdlTableConstraint[] | undefined;
  readonly label: string;
  constructor(schemaName: string, tableName: string, columns: readonly DdlColumn[], constraints?: readonly DdlTableConstraint[]);
  toOp(lowerer?: ExecuteRequestLowerer): Promise<Op>;
  renderTypeScript(): string;
  importRequirements(): readonly ImportRequirement[];
}
declare class DropTableCall extends PostgresOpFactoryCallNode {
  readonly factoryName: "dropTable";
  readonly operationClass: "destructive";
  readonly schemaName: string;
  readonly tableName: string;
  readonly label: string;
  constructor(schemaName: string, tableName: string);
  toOp(lowerer?: ExecuteRequestLowerer): Promise<Op>;
  renderTypeScript(): string;
  importRequirements(): readonly ImportRequirement[];
}
declare class AddColumnCall extends PostgresOpFactoryCallNode {
  readonly factoryName: "addColumn";
  readonly operationClass: "additive";
  readonly schemaName: string;
  readonly tableName: string;
  readonly column: DdlColumn;
  readonly label: string;
  constructor(schemaName: string, tableName: string, column: DdlColumn);
  toOp(lowerer?: ExecuteRequestLowerer): Promise<Op>;
  renderTypeScript(): string;
  importRequirements(): readonly ImportRequirement[];
}
declare class DropColumnCall extends PostgresOpFactoryCallNode {
  readonly factoryName: "dropColumn";
  readonly operationClass: "destructive";
  readonly schemaName: string;
  readonly tableName: string;
  readonly columnName: string;
  readonly label: string;
  constructor(schemaName: string, tableName: string, columnName: string);
  toOp(lowerer?: ExecuteRequestLowerer): Promise<Op>;
  renderTypeScript(): string;
  importRequirements(): readonly ImportRequirement[];
}
interface AlterColumnTypeOptions {
  readonly qualifiedTargetType: string;
  readonly formatTypeExpected: string;
  readonly rawTargetTypeForLabel: string;
  readonly using?: string;
}
declare class AlterColumnTypeCall extends PostgresOpFactoryCallNode {
  readonly factoryName: "alterColumnType";
  readonly operationClass: "destructive";
  readonly schemaName: string;
  readonly tableName: string;
  readonly columnName: string;
  readonly options: AlterColumnTypeOptions;
  readonly label: string;
  constructor(schemaName: string, tableName: string, columnName: string, options: AlterColumnTypeOptions);
  toOp(lowerer?: ExecuteRequestLowerer): Promise<Op>;
  renderTypeScript(): string;
  importRequirements(): readonly ImportRequirement[];
}
declare class SetNotNullCall extends PostgresOpFactoryCallNode {
  readonly factoryName: "setNotNull";
  readonly operationClass: "destructive";
  readonly schemaName: string;
  readonly tableName: string;
  readonly columnName: string;
  readonly label: string;
  constructor(schemaName: string, tableName: string, columnName: string);
  toOp(lowerer?: ExecuteRequestLowerer): Promise<Op>;
  renderTypeScript(): string;
  importRequirements(): readonly ImportRequirement[];
}
declare class DropNotNullCall extends PostgresOpFactoryCallNode {
  readonly factoryName: "dropNotNull";
  readonly operationClass: "widening";
  readonly schemaName: string;
  readonly tableName: string;
  readonly columnName: string;
  readonly label: string;
  constructor(schemaName: string, tableName: string, columnName: string);
  toOp(lowerer?: ExecuteRequestLowerer): Promise<Op>;
  renderTypeScript(): string;
  importRequirements(): readonly ImportRequirement[];
}
declare class SetDefaultCall extends PostgresOpFactoryCallNode {
  readonly factoryName: "setDefault";
  readonly operationClass: 'additive' | 'widening';
  readonly schemaName: string;
  readonly tableName: string;
  readonly columnName: string;
  readonly defaultSql: string;
  readonly label: string;
  constructor(schemaName: string, tableName: string, columnName: string, defaultSql: string, operationClass?: 'additive' | 'widening');
  toOp(lowerer?: ExecuteRequestLowerer): Promise<Op>;
  renderTypeScript(): string;
  importRequirements(): readonly ImportRequirement[];
}
declare class DropDefaultCall extends PostgresOpFactoryCallNode {
  readonly factoryName: "dropDefault";
  readonly operationClass: "destructive";
  readonly schemaName: string;
  readonly tableName: string;
  readonly columnName: string;
  readonly label: string;
  constructor(schemaName: string, tableName: string, columnName: string);
  toOp(lowerer?: ExecuteRequestLowerer): Promise<Op>;
  renderTypeScript(): string;
  importRequirements(): readonly ImportRequirement[];
}
/**
 * Planner-internal call for adding a NOT NULL column (no contract default) to
 * a table that must be empty at migration time. Carries the typed `DdlColumn`
 * and lowers it to an ADD COLUMN execute step via the adapter at `toOp` time.
 *
 * No authored `PostgresMigration` method: this call is only emitted by the
 * planner, never hand-written by migration authors.
 */
declare class AddNotNullColumnDirectCall extends PostgresOpFactoryCallNode {
  readonly factoryName: "rawSql";
  readonly operationClass: "additive";
  readonly schemaName: string;
  readonly tableName: string;
  readonly columnName: string;
  readonly column: DdlColumn;
  readonly label: string;
  constructor(schemaName: string, tableName: string, columnName: string, column: DdlColumn);
  toOp(lowerer?: ExecuteRequestLowerer): Promise<Op>;
  renderTypeScript(): string;
}
/**
 * Planner-internal call for adding a NOT NULL column (no contract default)
 * using a temporary default value for non-empty tables. Carries all parameters
 * needed for `buildAddNotNullColumnWithTemporaryDefaultOperation`; both the
 * typed ADD COLUMN execute step and the pre/postchecks are lowered via the
 * adapter at `toOp` time.
 *
 * No authored `PostgresMigration` method: this call is only emitted by the
 * planner, never hand-written by migration authors.
 */
declare class AddNotNullColumnWithTempDefaultCall extends PostgresOpFactoryCallNode {
  readonly factoryName: "rawSql";
  readonly operationClass: "additive";
  readonly schemaName: string;
  readonly tableName: string;
  readonly columnName: string;
  readonly column: StorageColumn;
  readonly codecHooks: Map<string, CodecControlHooks>;
  readonly storageTypes: Record<string, StorageTypeInstance>;
  readonly temporaryDefault: string;
  readonly label: string;
  constructor(options: {
    readonly schemaName: string;
    readonly tableName: string;
    readonly columnName: string;
    readonly column: StorageColumn;
    readonly codecHooks: Map<string, CodecControlHooks>;
    readonly storageTypes: Record<string, StorageTypeInstance>;
    readonly temporaryDefault: string;
  });
  toOp(lowerer?: ExecuteRequestLowerer): Promise<Op>;
  renderTypeScript(): string;
}
declare class AddPrimaryKeyCall extends PostgresOpFactoryCallNode {
  readonly factoryName: "addPrimaryKey";
  readonly operationClass: "additive";
  readonly schemaName: string;
  readonly tableName: string;
  readonly constraintName: string;
  readonly columns: readonly string[];
  readonly label: string;
  constructor(schemaName: string, tableName: string, constraintName: string, columns: readonly string[]);
  toOp(lowerer?: ExecuteRequestLowerer): Promise<Op>;
  renderTypeScript(): string;
  importRequirements(): readonly ImportRequirement[];
}
declare class AddUniqueCall extends PostgresOpFactoryCallNode {
  readonly factoryName: "addUnique";
  readonly operationClass: "additive";
  readonly schemaName: string;
  readonly tableName: string;
  readonly constraintName: string;
  readonly columns: readonly string[];
  readonly label: string;
  constructor(schemaName: string, tableName: string, constraintName: string, columns: readonly string[]);
  toOp(lowerer?: ExecuteRequestLowerer): Promise<Op>;
  renderTypeScript(): string;
  importRequirements(): readonly ImportRequirement[];
}
declare class AddForeignKeyCall extends PostgresOpFactoryCallNode {
  readonly factoryName: "addForeignKey";
  readonly operationClass: "additive";
  readonly schemaName: string;
  readonly tableName: string;
  readonly fk: ForeignKeySpec;
  readonly label: string;
  constructor(schemaName: string, tableName: string, fk: ForeignKeySpec);
  toOp(lowerer?: ExecuteRequestLowerer): Promise<Op>;
  renderTypeScript(): string;
  importRequirements(): readonly ImportRequirement[];
}
declare class DropConstraintCall extends PostgresOpFactoryCallNode {
  readonly factoryName: "dropConstraint";
  readonly operationClass: "destructive";
  readonly schemaName: string;
  readonly tableName: string;
  readonly constraintName: string;
  readonly kind: 'foreignKey' | 'unique' | 'primaryKey';
  readonly label: string;
  constructor(schemaName: string, tableName: string, constraintName: string, kind?: 'foreignKey' | 'unique' | 'primaryKey');
  toOp(lowerer?: ExecuteRequestLowerer): Promise<Op>;
  renderTypeScript(): string;
  importRequirements(): readonly ImportRequirement[];
}
declare class RenameCheckConstraintCall extends PostgresOpFactoryCallNode {
  readonly factoryName: "renameCheckConstraint";
  readonly operationClass: "widening";
  readonly schemaName: string;
  readonly tableName: string;
  readonly oldConstraintName: string;
  readonly newConstraintName: string;
  readonly label: string;
  constructor(schemaName: string, tableName: string, oldConstraintName: string, newConstraintName: string);
  toOp(lowerer?: ExecuteRequestLowerer): Promise<Op>;
  renderTypeScript(): string;
  importRequirements(): readonly ImportRequirement[];
}
declare class AddCheckConstraintCall extends PostgresOpFactoryCallNode {
  readonly factoryName: "addCheckConstraint";
  readonly operationClass: "additive";
  readonly schemaName: string;
  readonly tableName: string;
  readonly constraintName: string;
  /** Opaque SQL: the predicate body, rendered verbatim inside `CHECK (…)`. */
  readonly expression: string;
  readonly label: string;
  constructor(schemaName: string, tableName: string, constraintName: string, expression: string);
  toOp(lowerer?: ExecuteRequestLowerer): Promise<Op>;
  renderTypeScript(): string;
  importRequirements(): readonly ImportRequirement[];
}
declare class DropCheckConstraintCall extends PostgresOpFactoryCallNode {
  readonly factoryName: "dropCheckConstraint";
  readonly operationClass: "destructive";
  readonly schemaName: string;
  readonly tableName: string;
  readonly constraintName: string;
  readonly label: string;
  constructor(schemaName: string, tableName: string, constraintName: string);
  toOp(lowerer?: ExecuteRequestLowerer): Promise<Op>;
  renderTypeScript(): string;
  importRequirements(): readonly ImportRequirement[];
}
declare class CreateIndexCall extends PostgresOpFactoryCallNode {
  readonly factoryName: "createIndex";
  readonly operationClass: "additive";
  readonly schemaName: string;
  readonly tableName: string;
  readonly indexName: string;
  /** Column tuple when the index is column-based; absent for expression indexes. */
  readonly columns: readonly string[] | undefined;
  /** Opaque whole element list when the index is expression-based. */
  readonly expression: string | undefined;
  readonly indexType: string | undefined;
  readonly options: Record<string, unknown> | undefined;
  readonly where: string | undefined;
  readonly unique: boolean;
  readonly label: string;
  constructor(schemaName: string, tableName: string, indexName: string, elements: CreateIndexElements, extras?: CreateIndexExtras);
  toOp(lowerer?: ExecuteRequestLowerer): Promise<Op>;
  renderTypeScript(): string;
  importRequirements(): readonly ImportRequirement[];
}
declare class RenameIndexCall extends PostgresOpFactoryCallNode {
  readonly factoryName: "renameIndex";
  readonly operationClass: "widening";
  readonly schemaName: string;
  readonly tableName: string;
  readonly oldIndexName: string;
  readonly newIndexName: string;
  readonly label: string;
  constructor(schemaName: string, tableName: string, oldIndexName: string, newIndexName: string);
  toOp(lowerer?: ExecuteRequestLowerer): Promise<Op>;
  renderTypeScript(): string;
  importRequirements(): readonly ImportRequirement[];
}
declare class DropIndexCall extends PostgresOpFactoryCallNode {
  readonly factoryName: "dropIndex";
  readonly operationClass: "destructive";
  readonly schemaName: string;
  readonly tableName: string;
  readonly indexName: string;
  readonly label: string;
  constructor(schemaName: string, tableName: string, indexName: string);
  toOp(lowerer?: ExecuteRequestLowerer): Promise<Op>;
  renderTypeScript(): string;
  importRequirements(): readonly ImportRequirement[];
}
/**
 * Laundered pre-built operation.
 *
 * Wraps an already-materialized `SqlMigrationPlanOperation` — typically one
 * produced by a SQL-family method or a codec control hook — so the planner
 * can carry it alongside IR nodes without reverse-engineering it into a
 * structured call class. Doubles as the user-facing escape hatch for raw
 * migrations: authors can pass a full op shape to `rawSql({...})`.
 *
 * `toOp()` returns the stored op unchanged. `renderTypeScript()` emits
 * `rawSql({...})` with the op serialized as a JSON literal — round-tripping
 * requires every field on the op to be JSON-serializable (no closures).
 */
declare class RawSqlCall extends PostgresOpFactoryCallNode {
  readonly factoryName: "rawSql";
  readonly operationClass: MigrationOperationClass;
  readonly label: string;
  readonly op: Op;
  constructor(op: Op);
  toOp(): Op;
  renderTypeScript(): string;
}
declare class CreateExtensionCall extends PostgresOpFactoryCallNode {
  readonly factoryName: "createExtension";
  readonly operationClass: "additive";
  readonly extensionName: string;
  readonly label: string;
  constructor(extensionName: string);
  toOp(): Op;
  renderTypeScript(): string;
}
declare class CreateSchemaCall extends PostgresOpFactoryCallNode {
  readonly factoryName: "createSchema";
  readonly operationClass: "additive";
  readonly schemaName: string;
  readonly label: string;
  constructor(schemaName: string);
  toOp(lowerer?: ExecuteRequestLowerer): Promise<Op>;
  renderTypeScript(): string;
  importRequirements(): readonly ImportRequirement[];
}
declare class CreateNativeEnumTypeCall extends PostgresOpFactoryCallNode {
  readonly factoryName: "createNativeEnumType";
  readonly operationClass: "additive";
  readonly schemaName: string;
  readonly typeName: string;
  readonly members: readonly string[];
  readonly label: string;
  constructor(schemaName: string, typeName: string, members: readonly string[]);
  toOp(lowerer?: ExecuteRequestLowerer): Promise<Op>;
  renderTypeScript(): string;
  importRequirements(): readonly ImportRequirement[];
}
declare class DropNativeEnumTypeCall extends PostgresOpFactoryCallNode {
  readonly factoryName: "dropNativeEnumType";
  readonly operationClass: "destructive";
  readonly schemaName: string;
  readonly typeName: string;
  readonly label: string;
  constructor(schemaName: string, typeName: string);
  toOp(lowerer?: ExecuteRequestLowerer): Promise<Op>;
  renderTypeScript(): string;
  importRequirements(): readonly ImportRequirement[];
}
/**
 * `ALTER TYPE <qualified> ADD VALUE '<value>'` for a suffix-appended member on
 * a managed native enum. Built directly as SQL text (qualified via
 * `quoteQualifiedName`, the value escaped via `escapeLiteral`) rather than a
 * typed DDL node — mirrors how `enableRowLevelSecurity` renders its execute
 * step, since no DDL-AST node exists for this statement. Prechecks/postchecks
 * are still typed catalog queries lowered through the control adapter.
 * `validateEnumValueLength` runs at construction, so an over-length value
 * fails before planning produces a call.
 */
declare class AddNativeEnumValueCall extends PostgresOpFactoryCallNode {
  readonly factoryName: "addNativeEnumValue";
  readonly operationClass: "additive";
  readonly schemaName: string;
  readonly typeName: string;
  readonly value: string;
  readonly label: string;
  readonly summary: string;
  constructor(schemaName: string, typeName: string, value: string);
  toOp(lowerer?: ExecuteRequestLowerer): Promise<Op>;
  renderTypeScript(): string;
  importRequirements(): readonly ImportRequirement[];
}
/**
 * A planner-generated data-transform stub. `checkSlot` and `runSlot` name
 * the unfilled authoring slots that the rendered `migration.ts` will expose
 * to the user via `placeholder("…")` calls. `toOp()` always throws
 * `MIGRATION.UNFILLED_PLACEHOLDER`: the planner cannot lower a stubbed transform to a runtime
 * op — the user must fill the rendered `migration.ts` and re-emit.
 */
declare class DataTransformCall extends PostgresOpFactoryCallNode {
  readonly factoryName: "dataTransform";
  readonly operationClass: MigrationOperationClass;
  readonly label: string;
  readonly checkSlot: string;
  readonly runSlot: string;
  constructor(label: string, checkSlot: string, runSlot: string, operationClass?: MigrationOperationClass);
  toOp(): Op;
  renderTypeScript(): string;
  importRequirements(): readonly ImportRequirement[];
}
declare class CreatePostgresRlsPolicyCall extends PostgresOpFactoryCallNode {
  readonly factoryName: "createRlsPolicy";
  readonly operationClass: "additive";
  readonly schemaName: string;
  readonly tableName: string;
  readonly policy: PostgresRlsPolicy;
  readonly label: string;
  constructor(schemaName: string, tableName: string, policy: PostgresRlsPolicy);
  toOp(lowerer?: ExecuteRequestLowerer): Promise<Op>;
  renderTypeScript(): string;
  importRequirements(): readonly ImportRequirement[];
}
declare class DropPostgresRlsPolicyCall extends PostgresOpFactoryCallNode {
  readonly factoryName: "dropRlsPolicy";
  readonly operationClass: "destructive";
  readonly schemaName: string;
  readonly tableName: string;
  readonly policyName: string;
  readonly label: string;
  constructor(schemaName: string, tableName: string, policyName: string);
  toOp(lowerer?: ExecuteRequestLowerer): Promise<Op>;
  renderTypeScript(): string;
  importRequirements(): readonly ImportRequirement[];
}
declare class EnableRowLevelSecurityCall extends PostgresOpFactoryCallNode {
  readonly factoryName: "enableRowLevelSecurity";
  readonly operationClass: "additive";
  readonly schemaName: string;
  readonly tableName: string;
  readonly label: string;
  constructor(schemaName: string, tableName: string);
  toOp(lowerer?: ExecuteRequestLowerer): Promise<Op>;
  renderTypeScript(): string;
  importRequirements(): readonly ImportRequirement[];
}
declare class DisableRowLevelSecurityCall extends PostgresOpFactoryCallNode {
  readonly factoryName: "disableRowLevelSecurity";
  readonly operationClass: "destructive";
  readonly schemaName: string;
  readonly tableName: string;
  readonly label: string;
  constructor(schemaName: string, tableName: string);
  toOp(lowerer?: ExecuteRequestLowerer): Promise<Op>;
  renderTypeScript(): string;
  importRequirements(): readonly ImportRequirement[];
}
declare class RenamePostgresRlsPolicyCall extends PostgresOpFactoryCallNode {
  readonly factoryName: "renameRlsPolicy";
  readonly operationClass: "widening";
  readonly schemaName: string;
  readonly tableName: string;
  readonly oldPolicyName: string;
  readonly newPolicyName: string;
  readonly label: string;
  constructor(schemaName: string, tableName: string, oldPolicyName: string, newPolicyName: string);
  toOp(lowerer?: ExecuteRequestLowerer): Promise<Op>;
  renderTypeScript(): string;
  importRequirements(): readonly ImportRequirement[];
}
type PostgresOpFactoryCall = CreateTableCall | DropTableCall | AddColumnCall | DropColumnCall | AlterColumnTypeCall | SetNotNullCall | DropNotNullCall | SetDefaultCall | DropDefaultCall | AddNotNullColumnDirectCall | AddNotNullColumnWithTempDefaultCall | AddPrimaryKeyCall | AddForeignKeyCall | AddUniqueCall | AddCheckConstraintCall | RenameCheckConstraintCall | DropCheckConstraintCall | CreateIndexCall | RenameIndexCall | DropIndexCall | DropConstraintCall | RawSqlCall | CreateExtensionCall | CreateSchemaCall | CreateNativeEnumTypeCall | DropNativeEnumTypeCall | AddNativeEnumValueCall | CreatePostgresRlsPolicyCall | DropPostgresRlsPolicyCall | EnableRowLevelSecurityCall | DisableRowLevelSecurityCall | RenamePostgresRlsPolicyCall | DataTransformCall;
//#endregion
export { Op$1 as A, DropIndexCall as C, DropTableCall as D, DropPostgresRlsPolicyCall as E, RenamePostgresRlsPolicyCall as F, SetDefaultCall as I, SetNotNullCall as L, RawSqlCall as M, RenameCheckConstraintCall as N, EnableRowLevelSecurityCall as O, RenameIndexCall as P, DropDefaultCall as S, DropNotNullCall as T, DataTransformCall as _, AddNotNullColumnWithTempDefaultCall as a, DropColumnCall as b, AlterColumnTypeCall as c, CreateIndexCall as d, CreateIndexExtras as f, CreateTableCall as g, CreateSchemaCall as h, AddNativeEnumValueCall as i, PostgresOpFactoryCall as j, ForeignKeySpec as k, AlterColumnTypeOptions as l, CreatePostgresRlsPolicyCall as m, AddColumnCall as n, AddPrimaryKeyCall as o, CreateNativeEnumTypeCall as p, AddForeignKeyCall as r, AddUniqueCall as s, AddCheckConstraintCall as t, CreateExtensionCall as u, DisableRowLevelSecurityCall as v, DropNativeEnumTypeCall as w, DropConstraintCall as x, DropCheckConstraintCall as y };
//# sourceMappingURL=op-factory-call-BAeyLFYf-BAgG1h1C.d.mts.map