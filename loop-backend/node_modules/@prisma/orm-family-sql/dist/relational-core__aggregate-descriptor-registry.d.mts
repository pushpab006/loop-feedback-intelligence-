import { c as SqlAggregateDescriptorRegistry, f as isSqlAggregateDescriptor, l as SqlAggregateLowering, n as CodecDescriptorRegistry, s as SqlAggregateDescriptor, u as SqlAggregateLoweringContext } from "./query-lane-context-Qa-eddrE-kuZ8Z86-.mjs";
//#region ../../../2-sql/4-lanes/relational-core/dist/exports/aggregate-descriptor-registry.d.mts
//#region src/aggregate-descriptor-registry.d.ts
/**
 * Validate every contributed aggregate descriptor and settle its matches against the composed codec set.
 *
 * Validation covers the descriptor's own shape, the lowering rule — an operation outside the closed SQL aggregate alphabet must carry a `lower` hook, there being no plain `AggregateExpr` form for it — a second claim on one `(operation, input)` pair, and two trait descriptors that both claim a registered codec for one operation — the last of which only a composed stack can detect, which is why it is settled here rather than at contribution.
 */
declare function buildSqlAggregateDescriptorRegistry(descriptors: ReadonlyArray<unknown>, codecDescriptors: CodecDescriptorRegistry): SqlAggregateDescriptorRegistry;
//#endregion
export { SqlAggregateDescriptor, SqlAggregateLowering, SqlAggregateLoweringContext, buildSqlAggregateDescriptorRegistry, isSqlAggregateDescriptor };
//# sourceMappingURL=relational-core__aggregate-descriptor-registry.d.mts.map