import { n as CheckConstraintInput } from "./check-constraint-B7qGu6rc-HvQjjX82.mjs";
import { n as IndexElements, r as IndexInput } from "./sql-index-ByAAE7K8-DKj2V86p.mjs";
//#region ../../../2-sql/1-core/contract/dist/serialized-index-CrgG8ct1.d.mts
//#region src/serialized-check-constraint.d.ts
/**
 * A check constraint as `contract.json` stores it: full `name`, optional
 * `prefix` whose presence marks wire mode. The `table` entity kind hydrates it
 * through {@link checkConstraintInputFromSerialized}.
 */
interface SerializedCheckConstraint {
  readonly name: string;
  readonly prefix?: string;
  readonly expression: string;
}
/**
 * Hydrates one stored check constraint into constructor input. `parseNaming`
 * is what rejects a hand-edited file whose `prefix` and `name` disagree.
 */
declare function checkConstraintInputFromSerialized(flat: SerializedCheckConstraint): CheckConstraintInput;
//#endregion
//#region src/serialized-index.d.ts
/**
 * An index as `contract.json` stores it: full `name`, optional `prefix` whose
 * presence marks wire mode. The `table` entity kind hydrates it through
 * {@link indexInputFromSerialized}.
 */
type SerializedIndex = IndexElements & {
  readonly name: string;
  readonly prefix?: string;
  readonly where?: string;
  readonly unique: boolean;
  readonly type?: string;
  readonly options?: Record<string, unknown>;
};
/**
 * Hydrates one stored index into constructor input. The parameter is typed
 * but the values are whatever the file held, so each field the union depends
 * on is checked here rather than downstream — by the time `Index` sees the
 * data it is already a valid union.
 */
declare function indexInputFromSerialized(flat: SerializedIndex): IndexInput;
//#endregion
export { indexInputFromSerialized as i, SerializedIndex as n, checkConstraintInputFromSerialized as r, SerializedCheckConstraint as t };
//# sourceMappingURL=serialized-index-CrgG8ct1-DG3JpHc7.d.mts.map